class Utils
types
	public contractRole = Contract`contractRoles;
	public numForRole = map contractRole to nat1;
	public String = seq of char;
	public Date :: year: nat1
								month: nat1
								day: nat1
								inv mk_Date(y,m,d) == m <= 12 and d <= DaysOfMonth(y,m); --fun��o para verificar os dias de acordo com o m�s
values
-- TODO Define values here
instance variables
-- TODO Define instance variables here
operations
	public static pure DaysOfMonth: nat1*nat1 ==> nat1
	 DaysOfMonth(year,month) == (
		if month = 1 or month = 3 or month = 5 or month = 7 or month = 8 or month = 10 or month = 12 then
			return 31
		else if month = 2 then
			if (year mod 4 = 0 and year mod 100 <> 0) or (year mod 400 = 4) then
				return 29
			else
				return 28
		else
			return 30;
	)
functions

-- TODO Define functiones here
traces
-- TODO Define Combinatorial Test Traces here
end Utils